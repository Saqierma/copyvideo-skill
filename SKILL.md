---
name: viral-video-structure-replication
description: Use when a user provides or references a benchmark ecommerce short video and wants an original product-specific storyboard, shot script, voiceover, or AI video prompts; also use for 爆款带货视频拆解、爆款视频结构复刻、对标视频重构、分镜改写、口播重写、视频生成提示词，以及后续自然语言修改。
---

# 爆款视频结构复刻 Skill

## 核心原则

把对标视频转换为可复用的结构资产，再围绕用户自己的产品、人物和场景创作新视频。

1. 只学习节奏、镜头功能、情绪曲线、卖点顺序、转化逻辑和画面组织。
2. 不复用原文案、人物造型、品牌标识、商标、独特口头禅、具体案例或高识别度构图。
3. 用户的产品图、人物图、事实卖点、受众和使用场景高于对标视频。
4. 不虚构看不清、听不清或未提供的信息。使用 `未识别`、`画面推断` 或 `待用户确认` 标记不确定内容。
5. 不编造价格、折扣、销量、库存、认证、实验结果、医疗功效或绝对化承诺。
6. 所有结果使用 Markdown 表格或用户明确要求的 JSON，确保字段稳定、便于自动化处理。

## 输入检查

先检查以下输入：

| 输入 | 最低要求 | 缺失时处理 |
| --- | --- | --- |
| 对标视频 | 视频文件、可访问链接，或带时间轴的画面与转写 | 无法观察时不填写精确镜头和时间，说明限制并索取可分析材料 |
| 用户产品 | 产品图或明确的产品名称与外观描述 | 可完成原片分析和变量表；重构字段使用 `待提供` |
| 事实卖点 | 可公开使用的功能、材质、规格、价格等 | 不从对标视频迁移产品事实，不自行补充 |
| 人物与场景 | 人物图/描述、目标人群、使用场景 | 使用不带身份特征的通用占位描述 |
| 交付约束 | 平台、画幅、时长、语言、风格、模型 | 未提供时沿用原片画幅和近似时长，并明确假设 |

只在缺失信息会导致虚构或错误重构时提问。把必要问题合并成一次简短请求；能用明确占位符继续时直接继续。

## 分析流程

### 1. 获取可验证证据

使用当前可用的视频探测、抽帧、镜头切分、语音转写和字幕识别工具。至少交叉检查：

- 容器时长、分辨率和画幅；
- 镜头切点、转场和连续动作；
- 口播、画面字幕与产品展示；
- 结尾 CTA、价格或促销信息。

把识别结果分为 `直接观察`、`工具识别`、`合理推断`。不得把推断写成确定事实。

### 2. 建立基础分析

计算镜头数量和平均镜头时长，识别开头钩子、中段说服结构和结尾转化方式。核心卖点只记录原片实际表达的内容。

### 3. 按镜头拆解

以视觉切换、显著景别变化、镜头功能变化或连续动作结束作为分镜边界。每个镜头记录：

- 起止时间；
- 画面、口播和字幕；
- 人物动作、产品展示和镜头运动；
- 情绪、对应卖点与镜头作用。

同一连续镜头内的轻微动作不强行拆分。无法确认边界时使用近似时间并标记 `约`。

### 4. 提炼底层结构

把具体表达抽象为功能，例如：

`尴尬问题特写 → 放大痛点 → 产品介入 → 使用证据 → 降低顾虑 → 行动引导`

分别总结脚本结构、情绪曲线、镜头节奏、卖点顺序和转化逻辑。明确列出可复用的结构元素与不可照搬的识别性元素。

### 5. 建立变量替换表

逐项映射人物、产品、场景、卖点、对白、字幕、背景和 CTA。替换规则必须说明：

- 保留的是哪一种功能；
- 用户输入如何覆盖原片；
- 如何拉开具体表达距离；
- 缺失素材使用什么占位符。

### 6. 生成两个原创版本

**A. 严格对齐原视频框架版**

- 保留镜头数量、总时长、镜头功能、节奏密度和情绪曲线。
- 重写全部具体画面、人物、场景、口播、字幕和卖点。
- 不复刻高识别度布景、动作组合、视觉笑点或原片连续构图。

**B. 延展创新故事版**

- 只保留抽象说服逻辑和情绪推进。
- 按用户产品的真实使用链路重新设计故事、场景、镜头数量和表达。
- 优先解决目标人群的真实决策障碍，而不是机械对应原片。

### 7. 生成逐镜头模型提示词

每个提示词必须明确画面主体、人物动作、产品位置与可见状态、镜头运动、光线、环境、字幕、口播、情绪和负面要求。

产品图存在时，把它作为外观一致性参考：保持形状、颜色、材质、包装结构和可见文字位置稳定。不要让模型重设计产品。

负面要求至少覆盖：

- 不出现原视频人物、品牌、商标、包装或口头禅；
- 不复制原片高识别度画面和连续动作；
- 不生成第三方品牌、乱码字幕、错误产品结构或额外配件；
- 避免人物肢体畸形、产品穿模、尺寸跳变、镜间外观漂移；
- 不加入未经用户确认的功效、价格、折扣或合规声明。

### 8. 执行分镜与提示词一致性校验

把第五、六部分的分镜表作为第七部分最终视频模型提示词的唯一事实来源。

1. A 版和 B 版的每个分镜必须各自对应一个完整提示词；不得合并、拆分、遗漏或额外增加镜头。
2. 镜头编号、顺序和时长必须完全一致；各提示词时长之和必须等于对应版本的总时长。
3. `新画面内容`、`新口播`、`新字幕`、`产品展示` 和 `情绪` 必须逐字段进入对应提示词，不得改写成不同动作、不同文案或不同产品位置。
4. 分镜中的人物动作、场景和镜头运动分别映射到 `人物动作`、`背景环境` 和 `镜头运动`；分镜提示词中的光线与负面要求完整进入对应字段。
5. 最终提示词需要改变任何剧情、动作、时长、口播、字幕或产品展示时，先更新分镜表，再重新生成该版本的全部提示词。
6. 不得用“统一要求”“其余镜头同上”或一条汇总提示词代替逐镜头输出。

## 原创性检查

交付前逐项检查：

| 检查项 | 通过标准 |
| --- | --- |
| 文案距离 | 没有连续复用原片的特色句式、口头禅或字幕 |
| 视觉距离 | 没有复刻高识别度布景、人物造型和独特动作组合 |
| 品牌隔离 | 原片品牌、商标、包装和专属视觉系统不进入新方案 |
| 产品真实性 | 新卖点全部来自用户输入或明确标记为待确认 |
| 结构继承 | 能解释每个新镜头继承的功能，而非复制的内容 |
| 两版差异 | B 版不是 A 版换场景，而是重新组织原创故事 |
| 镜头完整性 | A/B 分镜与最终提示词的镜头编号、数量、顺序和时长一一对应 |
| 字段一致性 | 每个提示词的画面、动作、产品、运镜、字幕、口播和情绪与对应分镜逐字段一致 |

示例：原片用“办公室同事捂鼻”完成痛点社会证明，新片可以用“约会前在电梯镜面检查状态”承担同一镜头功能；不要保留同事、捂鼻动作、台词或构图。

## 输出契约

生成结果前必须读取 [references/output-contract.md](references/output-contract.md)，并严格使用其中的七段顺序和字段。

- 首次完整交付默认输出七个部分。
- 用户明确要求 JSON 或工作流接入时，使用参考文件中的 JSON 字段结构。
- 字段没有信息时填 `未提供`、`未识别` 或 `待确认`，不要删除列。
- 时长统一使用秒或 `MM:SS.mmm-MM:SS.mmm`，同一份输出保持一致。
- A 版镜头编号应与原片一一对应；B 版使用 `B01`、`B02` 等独立编号。

## 自然语言修改

把修改请求转换为约束差异，再更新受影响的分镜与最终提示词。

| 修改类型 | 必须联动更新 |
| --- | --- |
| 时长或镜头数量 | 时间段、镜头节奏、口播字数、全部提示词时长 |
| 人物或场景 | 画面、动作、背景、情绪、负面要求 |
| 产品或卖点 | 产品展示、口播、字幕、卖点顺序、合规措辞 |
| 风格或画幅 | 构图、景别、运镜、光线、环境和模型提示词 |
| 仅修改 A/B 版本 | 保持另一版本不变，并明确未改范围 |

默认重新输出被修改版本的完整分镜表和对应的最终视频模型提示词；用户要求全量结果时再重发七个部分。保留未被修改的事实约束，不让一次局部修改意外改变产品信息。

## 常见错误

| 错误 | 修正 |
| --- | --- |
| 把拆解写成内容摘要 | 按镜头记录功能、节奏、情绪和转化作用 |
| “严格对齐”变成逐句改写 | 只对齐时间、功能和情绪，全部具体表达重写 |
| A/B 两版高度相似 | B 版重建故事因果、场景和镜头组织 |
| 只写画面提示词 | 补齐动作、产品、运镜、光线、字幕、口播和负面要求 |
| 最终提示词与分镜对不上 | 以分镜表为唯一事实来源逐镜头生成；禁止合并镜头或省略字段 |
| 猜测听不清的台词 | 标记 `未识别`，必要时请求更清晰素材 |
| 沿用原片卖点到新产品 | 仅使用用户确认的产品事实 |
